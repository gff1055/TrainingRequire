requirejs(["helper/util"], function(util) {
    //This function is called when scripts/helper/util.js is loaded.
    //If util.js calls define(), then this function is not fired until
    //util's dependencies have loaded, and the util argument will hold
    //the module value for "helper/util".
    
    /*Esta funcao é chamada quando o arquivo util.js (dentro da pasta /scripts/helper) é carregado.
    Se util.js chama define(), entao esta funcao não é disparada até as
    ependencias de "util" serem carregadas, e o parametro util mantera o valor do modulo para "helper/util"
    */
    
    
});